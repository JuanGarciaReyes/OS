#include <stdio.h>
int counter=0; //count the number
int counter(int N);
int main() {
   // printf() displays the string inside quotation
   counter(10000);
   printf("%d\n", counter );

}

int counter(int N){
  for(int i =0;i<N;i++){
    counter++
  }
}
